{% extends 'base.html.twig' %}

{% block title %}Acceso Denegado{% endblock %}

{% block body %}
<div class="container mt-3" style="display: grid">
    <h2>
        {{ title }}
        <small class="text-body-secondary">{{ description }}</small>
    </h2>
    <img style="justify-self: center" src="{{ asset('imagen/access_denied.png') }}" class="d-block w-30" alt="access-denied">
    <p class="h3 mt-3 mb-3">Vuelva al <a href="{{ path('index') }}">Inicio</a></p>
</div>
{% endblock %}
